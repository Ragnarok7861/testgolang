version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - GO_ENV=production
    volumes:
      - /mnt/c/Projects/testgolang:/app
    command: ["./app"]

  db:
    image: postgres:alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 7861
      POSTGRES_DB: testgo
    ports:
      - "5432:5432"
